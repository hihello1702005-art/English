"use client";
import { useEffect, useRef, useState } from "react";
import { Mic, RotateCcw, Square, Volume2 } from "lucide-react";
export default function RecordingStudio(){const [recording,setRecording]=useState(false),[seconds,setSeconds]=useState(0),[message,setMessage]=useState("");const stream=useRef<MediaStream>(); useEffect(()=>{if(!recording)return;const id=setInterval(()=>setSeconds(v=>v+1),1000);return()=>clearInterval(id)},[recording]); async function start(){try{stream.current=await navigator.mediaDevices.getUserMedia({audio:true});setSeconds(0);setRecording(true);setMessage("Your microphone is active. Speak naturally.")}catch{setMessage("Microphone access is needed to record. Please allow it in your browser settings.")}} function stop(){stream.current?.getTracks().forEach(t=>t.stop());setRecording(false);setMessage("Recording saved locally. Connect a speech provider to analyse it.")} return <section className="studio"><div className="studio-top"><div><p className="eyebrow">SPEAKING PRACTICE</p><h2>Say it with confidence.</h2></div><button className="icon-button" aria-label="Listen to reference"><Volume2 size={18}/></button></div><blockquote>“Good morning everyone. Today we’re going to discuss effective classroom management.”</blockquote><div className={recording?"wave recording":"wave"} aria-label={recording?"Recording in progress":"Recording idle"}>{[...Array(13)].map((_,i)=><i key={i} style={{height:`${18+(i%5)*10}px`}}/>)}</div><div className="record-actions"><button className="secondary" onClick={()=>{setSeconds(0);setMessage("")}}><RotateCcw size={17}/>Try again</button>{recording?<button className="record stop" onClick={stop}><Square size={18}/>Stop · {String(Math.floor(seconds/60)).padStart(2,"0")}:{String(seconds%60).padStart(2,"0")}</button>:<button className="record" onClick={start}><Mic size={19}/>Start recording</button>}</div>{message&&<p className="studio-message" role="status">{message}</p>}</section>}
